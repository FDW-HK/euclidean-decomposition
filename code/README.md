# Code

This folder contains small reproducibility scripts for the Euclidean decomposition and support structures.

## Files

- `support_row.py` generates rows of the support triangle.
- `colored_support_row.py` generates rows of the colored support / multiplicity triangle.
- `cloud_chamber.py` generates square-grid support visualizations.

The scripts are intentionally minimal and follow the definitions directly.
